// Generated by LiveScript 1.5.0
(function(){
  var formulas;
  formulas = {
    metal: {
      price: function(level){
        return {
          metal: 60 * Math.pow(1.5, level),
          crystal: 15 * Math.pow(1.5, level)
        };
      },
      production: function(level){
        return 30 * level * Math.pow(1.1, level);
      },
      consumption: function(level){
        return 10 * level * Math.pow(1.1, level);
      }
    },
    crystal: {
      price: function(level){
        return {
          metal: 48 * Math.pow(1.6, level),
          crystal: 24 * Math.pow(1.6, level)
        };
      },
      production: function(level){
        return 20 * level * Math.pow(1.1, level);
      },
      consumption: function(level){
        return 10 * level * Math.pow(1.1, level);
      }
    },
    deut: {
      price: function(level){
        return {
          metal: 225 * Math.pow(1.5, level),
          crystal: 75 * Math.pow(1.5, level)
        };
      },
      production: function(level){
        return 10 * level * Math.pow(1.1, level) * (-0.002 * 250 + 1.28);
      },
      consumption: function(level){
        return 20 * level * Math.pow(1.1, level);
      }
    },
    solarplant: {
      price: function(level){
        return {
          metal: 75 * Math.pow(1.5, level),
          crystal: 30 * Math.pow(1.5, level)
        };
      },
      production: function(level){
        return 20 * level * Math.pow(1.1, level);
      }
    },
    roboticfactory: {
      price: function(level){
        return {
          metal: 400 * Math.pow(2, level),
          crystal: 120 * Math.pow(2, level),
          deut: 200 * Math.pow(2, level)
        };
      }
    },
    lab: {
      price: function(level){
        return {
          metal: 200 * Math.pow(2, level),
          crystal: 400 * Math.pow(2, level),
          deut: 200 * Math.pow(2, level)
        };
      }
    },
    shipyard: {
      price: function(level){
        return {
          metal: 400 * Math.pow(2, level),
          crystal: 200 * Math.pow(2, level),
          deut: 100 * Math.pow(2, level)
        };
      },
      buildings: {
        roboticfactory: 2
      }
    },
    energy: {
      isResearch: true,
      price: function(level){
        return {
          metal: 0,
          crystal: 800 * Math.pow(2, level),
          deut: 400 * Math.pow(2, level)
        };
      },
      buildings: {
        lab: 1
      }
    },
    combustionDrive: {
      isResearch: true,
      price: function(level){
        return {
          metal: 400 * Math.pow(2, level),
          crystal: 0,
          deut: 600 * Math.pow(2, level)
        };
      },
      buildings: {
        lab: 1
      },
      researches: {
        energy: 1
      }
    }
  };
  if (typeof window != 'undefined' && window !== null) {
    window['formulas'] = formulas;
  } else {
    module.exports = formulas;
  }
}).call(this);
