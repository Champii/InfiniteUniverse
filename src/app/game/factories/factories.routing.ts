import { Routes } from '@angular/router';

import { FactoriesComponent } from './factories.component';

export const FactoriesRoutes: Routes = [
  { path: 'factories', component: FactoriesComponent },
];

export const routedComponents = [FactoriesComponent];
